{
  "name": "ts-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm --filter @ts-monorepo/utils build && pnpm --filter @ts-monorepo/core build",
    "build:utils": "pnpm --filter @ts-monorepo/utils build",
    "build:core": "pnpm --filter @ts-monorepo/core build",
    "dev": "turbo run dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.6.4",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/node": "^25.0.10",
    "tsup": "^8.5.1",
    "turbo": "^2.7.5",
    "typescript": "^5.9.3"
  }
}
